def solution(a, b):
  

    return sum ( a[i]*b[i] for i in range(len(a)))
    
    
    
    
    """
    mulv = 0 
    for i in range(len(a)):
        
        mulv += a[i]*b[i]
    return mulv 
    """