#include <iostream>
#include <vector> 
#include <string> 
#include <algorithm> 
#include <cmath> 
#define jl "\n"
using namespace std; 
#define pi 3.1415926535897932
int main() { 
    
    cout << fixed;
    cout.precision(6);
    int r ; cin >> r ; 
    cout << pi*pow(r,2) << jl ; 
    cout << 2*pow(r,2) ;
    
} 
